<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xzx.xzxms.stock.dao.extend.StockContractAttributeExtendMapper">
    <select id="findContractHasContractAttributeByProId" resultType="PurchaseContract">
        SELECT
        pc.*
        FROM purchase_contract pc
        RIGHT JOIN stock_contract_attribute sca ON sca.contract_id = pc.id
        WHERE pc.is_active = 1 AND sca.is_active = 1
        <if test="projectId != null">
            AND pc.project_id = #{projectId}
        </if>
    </select>
</mapper>