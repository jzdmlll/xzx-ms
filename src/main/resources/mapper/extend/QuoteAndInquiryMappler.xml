<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xzx.xzxms.dao.extend.QuoteAndInquiry">
    <select id="findBySupplierOrPro" resultType="com.xzx.xzxms.bean.extend.QuoteExtendInquiry">
        select
          *
        from quote q
            left join inquiry i on q.inquiry_id=i.id
            left join sys_pro_detail d on d.id = i.pro_detail_id
        <where>
            q.is_active = 1 and i.is_active=1 and d.is_active = 1
            <if test="supplier!=-1">
                and q.supplier like %#{supplier,jdbcType=BIGINT}%
            </if>
            <if test="proId != -1">
                and d.id = #{proId,jdbcType=BIGINT}
            </if>
        </where>
    </select>
</mapper>